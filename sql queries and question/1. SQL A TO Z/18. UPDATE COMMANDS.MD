# Update command on table

Update - To update existing rows.

```sql
UPDATE table_name
SET col1 = val1, col2 = val2
WHERE condition;
```

- ex

```sql
UPDATE student
SET grade = "0"
WHERE grade = "A";
```
